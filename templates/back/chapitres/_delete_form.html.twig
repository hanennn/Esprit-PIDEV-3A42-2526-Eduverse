<form method="post" action="{{ path('app_chapitres_delete', {'id': chapitre.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chapitre.id) }}">
    <button type="submit">Supprimer</button>
</form>
