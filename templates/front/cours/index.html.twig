{% extends 'front/formateur.html.twig' %}

{% block title %}Cours et Chapitres{% endblock %}

{% block body %}
<h1>Liste des cours</h1>

{# 🔍 Formulaire de recherche et tri #}
<form method="get" class="mb-3 row g-2 align-items-end">
    <div class="col-md-4">
        <label>Critère de recherche</label>
        <select name="search_criteria" class="form-control">
            <option value="titre" {{ search_criteria == 'titre' ? 'selected' : '' }}>Titre</option>
            <option value="niv" {{ search_criteria == 'niv' ? 'selected' : '' }}>Niveau</option>
            <option value="matiere" {{ search_criteria == 'matiere' ? 'selected' : '' }}>Matière</option>
        </select>
    </div>
    <div class="col-md-5">
        <label>Recherche</label>
        <input type="text" name="search" class="form-control" placeholder="Recherche..." value="{{ search|default('') }}">
    </div>
    <div class="col-md-2">
        <label>Trier par</label>
        <select name="sort" class="form-control">
            <option value="titre" {{ sort == 'titre' ? 'selected' : '' }}>Titre</option>
            <option value="niv" {{ sort == 'niv' ? 'selected' : '' }}>Niveau</option>
        </select>
    </div>
    <div class="col-md-1">
        <button class="btn btn-primary w-100">OK</button>
    </div>
</form>

{# 🔹 Liste des cours en cartes (inchangée) #}
{% for cour in cours %}
<div class="card mb-3 p-3">
    <h5>{{ cour.titreCours }}</h5>
    <p>{{ cour.descCours }}</p>

    <!-- 🔹 Boutons Modifier / Supprimer Cours -->
    <div class="mb-2">
        <a href="{{ path('front_cours_edit', {id: cour.id}) }}" class="btn btn-sm btn-warning">Modifier</a>
        <form method="post" action="{{ path('front_cours_delete', {id: cour.id}) }}" style="display:inline-block;" onsubmit="return confirm('Supprimer ce cours ?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.id) }}">
            <button class="btn btn-sm btn-danger">Supprimer</button>
        </form>
    </div>

    <!-- Bouton pour afficher le formulaire Ajouter Chapitre -->
    <button class="btn btn-sm btn-success" onclick="toggleAddChapterForm({{ cour.id }})">
        + Ajouter Chapitre
    </button>

    <!-- Formulaire caché pour Chapitre -->
    <div id="addChapterForm-{{ cour.id }}" style="display: none;" class="mt-2">
        <form method="post" action="{{ path('front_chapitre_add', {id: cour.id}) }}" enctype="multipart/form-data">
            {{ form_widget(forms[cour.id]) }}
            <button type="submit" class="btn btn-primary btn-sm mt-2">Créer Chapitre</button>
        </form>
    </div>

    <!-- Liste des chapitres -->
    <ul class="mt-3">
        {% for chapitre in cour.chapitres %}
            <li>
                {{ chapitre.titreChap }}
                <a href="{{ path('front_chapitre_edit', {id: chapitre.id}) }}" class="btn btn-sm btn-outline-primary">Edit</a>
                <form method="post" action="{{ path('front_chapitre_delete', {id: chapitre.id}) }}" style="display:inline-block;">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chapitre.id) }}">
                    <button class="btn btn-sm btn-outline-danger">Supprimer</button>
                </form>
            </li>
        {% else %}
            <li>Aucun chapitre</li>
        {% endfor %}
    </ul>
</div>
{% else %}
<p>Aucun cours disponible</p>
{% endfor %}

<button class="btn btn-success mb-3" onclick="toggleAddCourseForm()">
    + Ajouter un cours
</button>
<!-- 🔹 Formulaire caché pour Ajouter un cours -->
<div id="addCourseForm" style="display: none;" class="mb-4">
    <form method="post" action="{{ path('front_cours_index') }}" enctype="multipart/form-data">
        {{ form_widget(formCours) }}
        <button type="submit" class="btn btn-primary mt-2">Créer Cours</button>
    </form>
</div>

{# 🔍 Formulaire de recherche et tri #}
<form method="get" class="mb-3 row g-2 align-items-end">
    <div class="col-md-4">
        <label>Critère de recherche</label>
        <select name="search_criteria" class="form-control">
            <option value="titre" {{ search_criteria == 'titre' ? 'selected' : '' }}>Titre</option>
            <option value="niveau" {{ search_criteria == 'niv' ? 'selected' : '' }}>Niveau</option>
            <option value="matiere" {{ search_criteria == 'matiere' ? 'selected' : '' }}>Matière</option>
        </select>
    </div>
    <div class="col-md-5">
        <label>Recherche</label>
        <input type="text" name="search" class="form-control" placeholder="Recherche..." value="{{ search|default('') }}">
    </div>
    <div class="col-md-2">
        <label>Trier par</label>
        <select name="sort" class="form-control">
            <option value="titre" {{ sort == 'titre' ? 'selected' : '' }}>Titre</option>
            <option value="niv" {{ sort == 'niv' ? 'selected' : '' }}>Niveau</option>
        </select>
    </div>
    <div class="col-md-1">
        <button class="btn btn-primary w-100">OK</button>
    </div>
</form>


{% endblock %}

{% block javascripts %}
{{ parent() }}
<script>
function toggleAddChapterForm(courseId) {
    const formDiv = document.getElementById('addChapterForm-' + courseId);
    formDiv.style.display = formDiv.style.display === 'none' ? 'block' : 'none';
}

// 🔹 Fonction pour afficher/cacher le formulaire Ajouter Cours
function toggleAddCourseForm() {
    const formDiv = document.getElementById('addCourseForm');
    formDiv.style.display = formDiv.style.display === 'none' ? 'block' : 'none';
}
</script>
{% endblock %}
